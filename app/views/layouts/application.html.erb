<!DOCTYPE html>
<html data-controller="theme">
  <head>
    <title><%= content_for(:title) || "Logicall" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100">
    <% if current_user %>
      <nav class="flex justify-between items-center px-6 py-4 border-b border-gray-100 dark:border-gray-700">
        <%= link_to "LOGICALL", dashboard_path, class: "text-sm font-medium tracking-wide" %>
        <div class="relative" data-controller="dropdown">
          <button type="button" data-action="click->dropdown#toggle" class="flex items-center gap-2 text-sm text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100">
            <%= current_user.email %>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded shadow-lg py-1 z-10">
            <% if current_user.owner? %>
              <%= link_to "Account settings", edit_account_path(current_user.account), class: "block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700" %>
            <% end %>
            <%= link_to "Profile", edit_account_user_path(current_user.account, current_user), class: "block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700" %>
            <button type="button" data-action="click->theme#toggle" class="w-full flex items-center justify-between px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700">
              <span>Dark mode</span>
              <span class="relative inline-flex h-5 w-9 items-center rounded-full bg-gray-300 dark:bg-blue-600 transition-colors">
                <span class="inline-block h-4 w-4 transform rounded-full bg-white shadow transition-transform translate-x-0.5 dark:translate-x-4"></span>
              </span>
            </button>
            <%= link_to "Log out", logout_path, data: { turbo_method: :delete }, class: "block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700" %>
          </div>
        </div>
      </nav>
    <% end %>

    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-full max-w-xs p-4 z-50">
      <% flash.each do |type, msg| %>
        <% if type == 'notice' %>
          <div class="p-4 mb-4 text-sm text-green-800 dark:text-green-200 rounded-lg bg-green-50 dark:bg-green-900" role="alert">
            <%= msg %>
          </div>
        <% elsif type == 'alert' %>
          <div class="p-4 mb-4 text-sm text-red-800 dark:text-red-200 rounded-lg bg-red-50 dark:bg-red-900" role="alert">
            <%= msg %>
          </div>
        <% else %>
           <div class="p-4 mb-4 text-sm text-blue-800 dark:text-blue-200 rounded-lg bg-blue-50 dark:bg-blue-900" role="alert">
            <%= msg %>
          </div>
        <% end %>
      <% end %>
    </div>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
